This directory serves as the AI context layer for persistent memory across chat interfaces.
Any architectural shifts, long-running workflow logs, and task outputs should be archived within this directory schema in the future to inform continuous iteration.
